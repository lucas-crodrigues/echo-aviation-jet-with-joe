---
import { Image } from "astro:assets";
import type { ImageMetadata } from "astro";

const { content } = Astro.props;

const images = import.meta.glob<{ default: ImageMetadata }>(
  "/src/assets/*.{jpeg,jpg,png,gif,webp}",
);
if (!images[content.imagePath]) {
  throw new Error(
    `"${content.imagePath}" does not exist in glob: "src/assets/*.{jpeg,jpg,png,gif,webp}"`,
  );
}
---

<section
  class="relative lg:mt-0 h-screen lg:h-[81vh] -z-0 bg-gray-800 flex flex-col items-end justify-center overflow-hidden mt-5"
>
  <Image
    alt=""
    src={images[content.imagePath]()}
    loading={"lazy"}
    class="absolute inset-0 -z-0 object-cover object-left-30 lg:object-top w-full h-full"
  />
  <div class="absolute bg-accent-500/20 w-full h-full z-0"></div>
  <div
    class="text-primary-700 py-12 pt-20 lg:mt-0 text-right z-10 flex flex-col items-end gap-4 px-5 h-full w-full lg:pt-28 justify-end"
  >
    <h3
      class="text-4xl md:text-4xl lg:text-6xl font-sans2 md:max-w-3xl lg:max-w-6xl font-black italic tracking-wide uppercase"
      set:html={content.headerH1}
    />
    <p
      class="text-base lg:text-xl mt-3 pr-4 font-semibold tracking-wide text-primary-950 max-w-5xl"
      set:html={content.paragraph}
    />
    <div
      class="mt-6 pr-4 w-full flex flex-wrap justify-end align-middle items-center gap-6 z-10"
    >
      {
        content.buttons.map((btn) => (
          <a
            href={btn.link}
            class={`${btn.primary ? "btn-primary" : "btn-accent"} w-full lg:px-12 text-2xl md:w-auto`}
          >
            {btn.name}
          </a>
        ))
      }
    </div>
  </div>
</section>
